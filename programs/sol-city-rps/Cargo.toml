[package]
name = "sol-city-rps"
version = "0.1.0"
description = "Solana City JoKenPo (Rock-Paper-Scissors) wager challenges — Private Ephemeral Rollups hide each player's choice until both have committed."
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "sol_city_rps"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
custom-heap = []
custom-panic = []
anchor-debug = []

[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed"] }
ephemeral-rollups-sdk = { version = "0.14.3", features = ["anchor", "access-control"] }
